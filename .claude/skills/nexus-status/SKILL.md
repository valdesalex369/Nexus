---
name: nexus-status
description: "WHEN the user asks for system status, health check, or what's running. Shows agents, memory, scheduler, Telegram, and platform connections."
---

# /nexus-status — Full System Status

Check `GET /api/health` and display: server status, all 7 agents, Telegram state, memory note count, evolution weights, scheduler state, platform connections.
